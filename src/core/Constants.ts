export enum PhotosPerSpread {
  LOW = 0,
  MEDUIM = 1,
  HIGH = 2,
  UNKNOWN = 3,
  POSTMEDIUM,
  PREHIGH
}

export enum DeliveryType {
  NONE = 0,
  HOME = 1,
  POST_OFFICE = 2
}

export enum Position {
  NONE = 0,
  LEFT = 1,
  RIGHT = 2
}

export enum Orientation {
  LANDSCAPE = 0,
  PORTRAIT = 1
}
